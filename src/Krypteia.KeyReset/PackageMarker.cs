namespace Krypteia.KeyReset;

/// <summary>
/// Marker type for the Krypteia.KeyReset package.
/// Implementations of <see cref="Abstractions.IKeyResetService"/> will be added in a follow-up commit.
/// </summary>
internal static class PackageMarker
{
    // Intentionally empty. This file exists so the project compiles and so the
    // PackageReadmeFile from Directory.Build.props can be embedded into the package.
}
